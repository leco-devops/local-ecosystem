# UI credential vault for LEco DevOps (local dev only).
# Copy to config/ui-credentials.yaml and customize. Gitignored — never commit.
# Managed via Service hubs → UI access and per-service hub actions.
# Mutations require DASHBOARD_CONTROL_TOKEN when set.

version: 1
services:
  minio:
    username: minioadmin
    password: minioadmin
  mysql:
    driver: server
    server: mysql
    username: root
    password: localdev
    database: ""
  postgres:
    driver: pgsql
    server: n8n_postgres
    username: postgres
    password: password
    database: n8n
  n8n:
    email: admin@local.lh
    password: Localdev1
  webui:
    email: admin@local.lh
    password: Localdev1
    name: Admin Local
  sftp:
    username: leco
    password: leco#localhost-192
    port: "2222"
    auth_mode: password
    public_key: ""
  ftp:
    username: leco
    password: leco#localhost-192
    port: "21"
