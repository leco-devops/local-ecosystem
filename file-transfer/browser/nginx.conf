server {
    listen 8080;
    server_name _;
    root /srv;
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    # Read-only browser: list and download only (volume is also mounted :ro).
    location / {
        limit_except GET HEAD {
            deny all;
        }
    }
}
