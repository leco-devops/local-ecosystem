{
  "mcpServers": {
    "leco-devops": {
      "type": "stdio",
      "command": "${CLAUDE_PROJECT_DIR:-.}/tools/claude-plugin/bin/leco-mcp-launch",
      "args": ["stdio"],
      "env": {
        "LECO_MCP_ALLOW_DESTRUCTIVE": "${LECO_MCP_ALLOW_DESTRUCTIVE:-0}",
        "LECO_MCP_ALLOW_CREDENTIALS": "${LECO_MCP_ALLOW_CREDENTIALS:-0}"
      }
    }
  }
}
